local filter = class:extend("filter")
-- {"solid"="slide"}
function filter:init(...)
  -- i haven't quite fleshed out the whole idea/plan for this
-- something like:
--other = {table of collisionFlags the entity responds to}
--response = {how the collision is handled}
  local filter = {}

  
end
return filter
-- {solid = "removeSelf", player = "damage"}
